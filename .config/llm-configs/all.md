- if you output a (draft) replies:
    - always store the draft reply as a separate file in
      `/tmp/llm-draft-reply-<CURRENT-POSIX-TIMESTAMP>.md`
    - always output it as raw markdown that I can copy&paste, but don't manually
      line break long lines just when they are long
    - never output symbol like `—` etc. that immediately hint that the response
      was generated by an LLM

- whenever you review something:
    - do not report nits/nitpicks - concentrate on blocking issues or potential
      real issues only - we don't care for small/unimportant issues - if there
      are changes or checks you propose, output them as raw markdown that I can
      copy&paste as a response to people, but don't manually line break long
      lines just when they are long

    - verify that the repositories you are looking at are on the right branch
      and up-to-date (if not you can run `gru`)

- never reference specific Jenkins builds/build-numbers in documentation or code, they
  are ephemeral - referencing Jobs is fine tho

- whenever you do changes, at the very end of your response to me, list all the
  local paths in which you have done changes one line after another

- always add comments to code except for very trivial or self-evident code

- if doing changes in a git repository:
    - Every finished contiguous change done must be committed separately via 'git'
        + Only stage the changes you made
        + The first git commit message line must be concise
        + The rest of the git commit message must be thorough and smart wrap at 72
        characters

- All local repository checkouts live under `~/workspace`. For repositories,
  projects, or codebases without giving a path, look for it under
  `~/workspace/<repo-name>` first.
