#!/usr/bin/env bash
set -eo pipefail

path=$1
shift

args=(
    --standalone
    --css=markdown.css
    --toc
)

case "$1" in
-s)
    shift
    fname="${path%.*}"
    args+=(
        -o "${fname}.html"
    )
    ;;
esac

pandoc "$path" "${args[@]}"
