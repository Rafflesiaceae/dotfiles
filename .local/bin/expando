#!/usr/bin/env bash
set -eo pipefail
if git-is-a-git-workspace; then
    echo "+ git-wip"
    git-wip
fi
echo "+ cb | oat filetree expand"
cb | oat filetree expand
if git-is-a-git-workspace; then
    echo "+ git-wip"
    git-wip
fi
