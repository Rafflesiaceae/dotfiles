#!/usr/bin/env bash
set -eo pipefail
echo "+ git-wip-commitpush -np"
git-wip-commitpush -np
echo "+ cb | oat filetree expand"
cb | oat filetree expand
echo "+ git-wip-commitpush -np"
git-wip-commitpush -np
