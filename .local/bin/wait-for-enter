#!/bin/dash
msg="Press Enter to continue..."
case "$1" in
*...)
    msg="$1"
    ;;
"") ;;
*)
    msg="Press Enter to continue $1..."
    ;;
esac

printf "%s" "$msg"
IFS= read -r _ 2>/dev/null || true
