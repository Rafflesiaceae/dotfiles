#!/usr/bin/env bash
# secret-lookup v0.1 (2022-08-28) (50b2d8c474e098a9)
#
# Simple shim around `secret-tool` to lookup a stored secret.
#
# Requires: secret-tool
set -eo pipefail
secret-tool lookup id "$1"
