#!/usr/bin/env bash
set -eo pipefail
set -x

"docker"
docker-compose rm --all --force --stop --volumes || true

docker system prune --all -f
docker volume prune -f

: "podman"
podman rm -a -f
# podman container prune
podman system prune --all -f
podman volume prune -f
