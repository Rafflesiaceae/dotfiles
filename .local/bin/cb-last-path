#!/usr/bin/env bash
set -eo pipefail

lastpath=$(cat ~/.cache/zsh/dirs | head -n 1)
if [[ -t 1 ]]; then
    echo "Copied: ${lastpath}"
else
    notify-send --urgency=low "[cb-last-path]" "$lastpath"
fi
cb -s "$lastpath"
